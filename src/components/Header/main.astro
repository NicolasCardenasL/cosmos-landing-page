---
import ResponsiveMenuIcon from "../../../header/icons/ResponsiveMenuIcon.astro";
import ResponsiveNav from "./ResponsiveNav.astro"
// import ResponsiveSideNav from "./ResponsiveSideNav.astro"
import OdysseyLogo from "../OdysseyLogo.astro"


---
<header>
    <OdysseyLogo></OdysseyLogo>
    <ResponsiveNav></ResponsiveNav>
    <!-- <ResponsiveMenuIcon className="menuIcon"></ResponsiveMenuIcon> -->
    <!-- <ResponsiveSideNav></ResponsiveSideNav> -->
</header>
<style>
    header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
    }
    /* a {
        font-weight: var(--font-weight-semibold);
        text-decoration: none;
        color: var(--text-black)
    }
    ul {
        list-style-type: none;
        display: flex;
    }
    .navList li a {
        padding: 18px 30px;
    }
    .navList li:last-child a {
        cursor: pointer;
        border-radius: 20px;
        color: var(--text-white);
        background-color: var(--bg-color-black);
        position: relative;
        user-select: none;
    }
    .navList li:last-child:hover {
        transition: transform 0.1s ease-out;
        transform: translateY(-4px);
    }
    .navList li:last-child a:hover {
        box-shadow: 0 6px 7px rgba(83, 100, 255, 0.32);
        border: black 1px;
        background-color: #FFFF;
        color: black;
        transition: all 0.1s ease-out;
    }
    .navList li:last-child:active {
        transform: translateY(2px);
        transition: all 0.1s ease-out;
    }
    img, svg {
        user-select: none;
    } */
</style>